# Распознавание музыки

Страница слушает микрофон или звук вкладки, сама замечает, что заиграла музыка,
и распознаёт трек через [AudD](https://docs.audd.io/). Без сборки и без бэкенда —
статические файлы, работают с GitHub Pages как есть.

## Почему не streams API

[Streams API](https://docs.audd.io/streams) распознаёт радиопотоки по URL на стороне
AudD и стоит $45 в месяц за поток. Здесь источник звука — устройство пользователя,
URL у него нет, так что используется обычный файловый эндпоинт с короткими клипами.

## Почему не «записать трек целиком»

Отправлять песню от начала до конца — значит ждать результат три-четыре минуты.
AudD хватает 10–15 секунд, поэтому клип уходит через ~10 секунд после начала музыки,
а ответ приходит примерно за секунду.

## Как устроено

```
микрофон / вкладка
  ├─ AnalyserNode ──→ MusicDetector ──→ MusicGate ──→ сессия ──→ AudD
  └─ AudioWorklet ──→ кольцевой буфер 30 с ──→ WAV 16 кГц моно ──┘
```

**`js/audio.js`** — захват и буфер. Все три штатные обработки микрофона отключены
(`echoCancellation`, `noiseSuppression`, `autoGainControl`): шумодав съедает музыку,
AGC качает уровень, а эхоподавление вычтет звук собственных колонок. Звук копится
в кольцевом буфере на 30 секунд, поэтому клип можно взять «задним числом» — начиная
с момента до того, как детектор успел сработать. Перед отправкой буфер сводится
в 16 кГц моно WAV: акустические отпечатки живут ниже 5 кГц, так что этого с запасом,
а весит втрое меньше.

Кадр анализа привязан к приходу звука из ворклета, а не к `setInterval` — таймеры
в фоновой вкладке душатся до 1 Гц, аудиопоток нет.

**`js/detector.js`** — четыре признака, каждый закрывает свой класс ложных
срабатываний: громкость над шумовым фоном (тишина), спектральная плоскостность
(шипение, вентилятор), доля баса (речь), непрерывность (паузы между фразами).

Шумовой фон оценивается по минимальной статистике: вниз следуем быстро, вверх —
медленно и только пока музыка не играет. Симметричное окно тут не работает — за
минуту непрерывного трека фон дорастает до уровня самой музыки, превышение падает
до нуля и детектор теряет песню на середине.

`MusicGate` добавляет гистерезис: вход в состояние «музыка» требует 2.5 с подряд
выше порога, выход — 5 с ниже. Иначе пауза между куплетами обрывала бы сессию.

**`js/app.js`** — состояния и расписание запросов. AudD платный, поэтому:

- первый запрос только после ~10 секунд устойчивой музыки;
- если совпадений нет — три повтора (12 / 22 / 40 с), потом молчим до следующего трека;
- пока трек играет, повторно не спрашиваем;
- смена трека без паузы (радио, плейлист) ловится по расписанию, которое считается
  из `timecode` и длительности текущего трека: просыпаемся ровно к его концу.

## Измерено

Синтетический прогон на реальной записи (40 секунд непрерывной музыки против тихого
шума комнаты):

| | музыка | шум |
|---|---|---|
| пик оценки | 1.00 | 0.24 |
| срабатывание гейта | 2.8 с | нет |
| минимум за 40 с игры | 0.94 | — |

Порог по умолчанию — 0.55.

## Ключ

Ключа в коде нет — страница публичная, и зашитый ключ тратили бы все подряд. При
первом заходе блок настроек раскрыт, ключ вводится вручную и живёт в `localStorage`
этого браузера. На каждом устройстве — свой.

Завести ключ: [dashboard.audd.io](https://dashboard.audd.io/).
